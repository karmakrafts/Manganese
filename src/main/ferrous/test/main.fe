extern fun printf(fmt: *char, ...): i32

mod foo {
    pub struct Value {
        pub mut x: i32
    }

    pub struct Vector2i {
        pub x: Value
        pub y: Value
    }
}

fun test(value: i32): i32 {
    return value << 1 * value
}

fun fill(value: i32): i32 {
    let mut vec: foo::Vector2i
    let mut x = 22
    let mut y: mut &i32 = x
    y = y << (2 >> 1)
    vec.x.x = y
    vec.y.x = vec.x.x >> 1
    let vec2 = vec
    return +-(vec2.x.x + vec2.y.x) + value
}

fun test2(value: &bool): &bool {
    return value
}

fun main(num_args: i32, args: **i8): i32 {
    return fill(num_args)
}