@NoMangle
fun main(num_args: i32, args: char**): i32 {
    let mut i = 0
    i += 1
    return i

    let x = num_args
    let mut y = &x
    let mut f = 2.0
    let mut i_f = -f
    let mut z = &y
    let mut cond = false
    let mut atom: atomic i32
    let ptr = !cond ? *z : y

    return x == 1 ? *ptr : 0
}