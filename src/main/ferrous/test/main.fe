extern fun printf(fmt: *char, args: vaargs): i32

mod foo {
    pub struct Vector2i {
        pub mut x: i32
        pub mut y: i32
    }
}

fun test(value: i32): i32 {
    return value << 1 * value
}

fun fill(value: i32): i32 {
    let mut vec: foo::Vector2i
    let mut x = 22
    let mut y: mut &i32 = x // Copy value of x at this point
    y = y << (2 >> 1)
    vec.x = y
    vec.y = vec.x >> 1
    return vec.x + vec.y
}

fun test2(value: &bool): &bool {
    return value
}

fun main(num_args: i32, args: **i8): i32 {
    return fill(0)
}