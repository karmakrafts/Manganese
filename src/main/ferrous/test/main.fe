extern fun printf(fmt: *char, ...): i32

mod foo {
    pub struct Value {
        pub mut x: i32
    }

    pub struct Vector2i {
        pub mut x: Value
        pub mut y: Value
    }
}

fun test(value: i32): i32 {
    return value << 1 * value
}

fun fill(value: i32): i32 {
    let mut vec: foo::Vector2i
    let mut x = 22
    let mut y: mut &i32 = x
    y = y << (2 >> 1)
    vec.x.x = y
    vec.y.x = vec.x.x >> 1
    let mut vec2 = vec
    return +-(vec.x.x + vec.y.x)
}

fun test2(value: &bool): &bool {
    return value
}

fun main(num_args: i32, args: **i8): i32 {
    return fill(0)
}