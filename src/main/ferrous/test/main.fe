extern fun printf(fmt: *char, args: vaargs): i32

mod foo {
    struct Vector2f {
        x: f32
        y: f32
    }
}

fun fill(value: *foo::Vector2f): bool {
    let mut x: foo::Vector2f = ?
    return false
}

fun test(value: i32): i32 {
    let mut x = value
    x = x * x
    return 1 << x
}

fun main(num_args: i32, args: **i8): i32 {
    let mut x = 10
    x = x * x
    return x
}